spring:
  application:
    name: finance-management
  datasource:
    url: jdbc:postgresql://localhost:5433/finance_management
    username: postgres
    password: Su3@na03
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080